# Nexus

## Key Points

- **run on java**
- **use to store and manage artifacts**
- **supports multiple formats (maven, npm, docker, etc.)**
- **use as package manager** (as a Proxy → cache artifacts from public repos (e.g., Maven Central, npm registry, Docker Hub).)
