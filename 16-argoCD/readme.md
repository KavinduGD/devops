# ArgoCD

- Argo CD is a declarative, GitOps continuous delivery tool for Kubernetes.
- It continuously monitors running applications and compares their current, live state against the desired target state (as specified in the Git repo)
- If a divergence is detected, Argo CD can automatically or manually sync the live state back to the desired target state.
