# Vagrant

## Main settings

    - private network settings
    - public network settings
    - memory
    - cpus

## Provisioning

    - same as aws user data
    - execute only once
    - if you want to run provisioning again, you need to provide `--provision` flag
